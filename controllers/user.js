

const user = {
  logIn: async (ctx, next) => { 

  },
  logOut: async (ctx, next) => { 

  },
  isLogIn: async (ctx, next) => { 

  },
  getUserInfo: async (ctx, next) => { 

  },
};

export default user;